create table public.continente (
  id bigint generated always as identity not null,
  nome text not null,
  constraint continente_pkey primary key (id),
  constraint continente_nome_key unique (nome)
) TABLESPACE pg_default;
